import { InteresCompuestoPropio } from './interes-compuesto-propio';

describe('InteresCompuestoPropio', () => {
  it('should create an instance', () => {
    expect(new InteresCompuestoPropio()).toBeTruthy();
  });
});
